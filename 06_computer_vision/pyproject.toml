[project]
name = "06-computer-vision"
version = "0.1.0"
description = "Computer Vision learning project"
readme = "README.md"
authors = [
    { name = "pmake", email = "unique_ntsn@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anylabeling>=0.4.36",
    "easyocr>=1.7.2",
    "ultralytics>=8.4.131",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
