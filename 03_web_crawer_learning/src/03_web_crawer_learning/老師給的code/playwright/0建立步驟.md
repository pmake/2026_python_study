# 我是用uv來安裝，"uv add playwright
# 另外3行是另外安裝一個瀏覽器套件，檔案很大，也可以不用安裝，使用時可以指定要用的瀏覽器為系統原本安裝的瀏覽器。

-一定要安裝最新版 要不然用不了chrome
-pip install playwright
-playwright install
-playwright install chromium
-playwright install chromium --with-deps