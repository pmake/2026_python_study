[project]
name = "04-data-visualization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pmake", email = "unique_ntsn@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "plotly>=6.9.0",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.13.0"]
build-backend = "uv_build"
