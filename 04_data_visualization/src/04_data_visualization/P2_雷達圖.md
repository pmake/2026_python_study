- prompt:
    - 從@database.py import supabase物件後，從"players" table join "career_summaries" table, "person_id"做為join key，用inner join
        - 以firstName篩選"LeBron","Carmelo"，選取all column
    - 然後用plotly 畫 line_polar
        - 數值 ppg, rpg, apg, bpg, mpg
            - 各欄位的數值，根據 0 - 各欄位的最大值轉換成0-10的數值 再畫圖
                - 各欄位的最大值請以所有球員的最大值為準
        - 最後 fig.show()